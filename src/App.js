import React from "react";
import ReactDOM from "react-dom";

const App = () => {
  return <React.StrictMode>Omar Sattaur</React.StrictMode>;
};

ReactDOM.render(<App />, document.getElementById("root"));
